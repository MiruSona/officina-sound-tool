# 할 일 : 사운드 툴

> 담당 범위 : SFX · BGM
> 전체 색인은 `Docs/Todo/진행상황.md`

## 이 툴의 좋은 점

**그림과 아무 상관이 없다.** 그림체나 시점 결정을 안 기다려도 되고,
조사도 사실상 끝나 있다. **지금 당장 설계·작업에 들어갈 수 있는 유일한 툴이다.**

## 1. 정해진 것

| 항목 | 결정 | 근거 |
| --- | --- | --- |
| SFX 엔진 | **rfxgen** (zlib 라이선스) | `Research/2026-08-20-SFX엔진재조사.md` |
| BGM 엔진 | **mido → FluidSynth → GeneralUser GS** — 전부 명령줄로 이어진다 | `Design/2026-08-20-그림사운드툴결정사항.md` |
| SFX·BGM 통합 | **안 한다. 따로 간다.** Unity 에서도 따로 다룬다 | 〃 |
| 악보 문법 | abc notation 안 씀 (사람이 악보를 쓸 일이 없다) | 〃 |
| 소리 검수 | **코드로 한다** — librosa + FFT | 〃 |
| 파라미터 만들기 | Qwen + llama.cpp GBNF 로 스키마 강제. **새 모델 0개** | 〃 |
| 큰 원칙 | **AI 는 짧은 JSON 만 뱉고, 실제 결과물은 코드가 만든다** | 〃 |

### rfxgen 을 고른 이유

- `.rfx` 는 **104바이트 고정 바이너리** (헤더 8 + WaveParams 96 = int 2 + float 22). **포맷 분석이 끝났다.**
- Python `struct.pack` 으로 만들고 `rfxgen --input a.rfx --output a.wav` 로 굽는다. 두 단계뿐이다.
- 2016년 이후 포맷이 안 바뀌었고, 2026-08-13 에도 커밋이 있다. 공식 Windows x64 바이너리 제공.
- **pyfxr 은 실제로 못 쓴다** — 이 PC 가 Python 3.14 인데 wheel 이 3.13 까지만 있다.

### 장기 안전판

외부 실행파일 의존을 없애고 싶어지면 rfxgen 의 `GenerateWave`(약 310줄)를 Python 으로 포팅한다.
외부 의존성 없는 순수 C 이고 zlib 라이선스라 포팅이 허용된다. **지금 할 필요는 없다.**

## 2. 다음에 할 일

- [ ] **`.rfx` 엔디안 실물 확인** — 파일 하나 만들어 보면 끝난다. 제일 먼저 할 것
- [ ] SFX 툴 설계 — JSON 스키마, CLI 형태, 프리셋 목록
- [ ] BGM 툴 설계 — mido 로 만들 MIDI 구조, FluidSynth 호출
- [ ] 소리 검수 기준 정하기 (길이·최대 음량·주파수 분포)
- [ ] 산출물은 `Docs/Design/` 에

## 3. 아직 못 정한 것

- `.rfx` 엔디안 (위 첫 항목에서 바로 풀린다)
- GeneralUser GS 사운드폰트의 배포 조건 — 키트에 같이 넣어도 되는지 확인 필요
- 어떤 SFX 프리셋이 필요한지 (게임 장르에 달림)

## 4. 설치가 필요한 것

승인받고 설치한다.

- rfxgen 공식 Windows x64 바이너리
- FluidSynth, GeneralUser GS 사운드폰트
- Python : mido, librosa
